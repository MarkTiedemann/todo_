install:
  - ps: iex (new-object net.webclient).downloadstring('https://get.scoop.sh')
  - scoop install make rustup rust
  - rustup component add rustfmt
build: off
test_script:
  - make -f Makefile.win
